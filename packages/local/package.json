{
  "name": "@agent-ix/ix-cli-local",
  "description": "Local cluster commands for ix-cli (up, down, init-cluster, auth).",
  "version": "0.6.0",
  "type": "module",
  "files": [
    "dist/",
    "README.md"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./plugin": {
      "types": "./dist/index.d.ts",
      "default": "./dist/plugin.js"
    }
  },
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "test": "vitest run",
    "test:json": "vitest run --reporter=json --outputFile=report.json || true",
    "lint": "eslint \"src/**/*.ts\" --no-error-on-unmatched-pattern \"tests/**/*.ts\" && pnpm run format:check",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build": "vite build",
    "clean": "rm -rf dist coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/agent-ix/ix-cli.git"
  },
  "dependencies": {
    "@agent-ix/ix-cli-core": "0.10.1-20260614.214507-9d72fe4",
    "@agent-ix/ix-ui-cli": ">=0.4.6",
    "@clack/prompts": ">=1.1.0",
    "cli-table3": ">=0.6.5",
    "execa": ">=9.6.1",
    "listr2": ">=10.2.1",
    "picocolors": ">=1.1.1",
    "react": ">=18.3.1",
    "yaml": ">=2.8.3",
    "zod": ">=4.0.0"
  },
  "devDependencies": {
    "@types/node": ">=25.6.0",
    "@types/react": ">=18.3.12",
    "vite": ">=6.3.4",
    "vite-plugin-dts": ">=4.5.3",
    "vitest": ">=3.1.2"
  }
}
