{
  "name": "@agent-ix/ix-cli-elements",
  "description": "Element discovery, tap management, and project scaffolding commands for ix-cli.",
  "version": "0.6.0",
  "type": "module",
  "files": [
    "dist/",
    "README.md"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "test": "vitest run",
    "test:json": "vitest run --reporter=json --outputFile=report.json || true",
    "lint": "eslint \"src/**/*.ts\" --no-error-on-unmatched-pattern \"tests/**/*.ts\" && pnpm run format:check",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build": "vite build",
    "clean": "rm -rf dist coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/agent-ix/ix-cli.git"
  },
  "dependencies": {
    "@agent-ix/ix-cli-core": "workspace:*",
    "@agent-ix/ix-ui-cli": "^0.4.4",
    "@clack/prompts": ">=1.1.0",
    "execa": ">=9.6.1",
    "picocolors": ">=1.1.1",
    "react": ">=18.3.1",
    "yaml": ">=2.8.3"
  },
  "devDependencies": {
    "@types/react": ">=18.3.12",
    "vite": ">=6.3.4",
    "vite-plugin-dts": ">=4.5.3",
    "vitest": ">=3.1.2"
  }
}
